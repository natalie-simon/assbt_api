name: Deploy NestJS to OVH VPS (Production)

on:
  push:
    branches:
      - production
    paths-ignore:
      - '**.md'
      - '.github/ISSUE_TEMPLATE/**'
  workflow_dispatch:  # permet de déclencher manuellement

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Pas besoin de créer un fichier .env, nous utiliserons celui déjà sur le VPS

      - name: Deploy to OVH VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: deploy
          key: ${{ secrets.SSH_PRIVATE_KEY_OVH }}
          port: 2222  # ou votre port SSH personnalisé
          script: |
            # Se rendre dans le répertoire de déploiement
            cd ~/app

            # Sauvegarde de la base de données si l'application est déjà en cours d'exécution
            if [ -d "~/app/nestjs" ] && [ -f "~/app/nestjs/docker-compose.prod.yml" ]; then
              cd ~/app/nestjs
              if docker-compose -f docker-compose.prod.yml ps postgres 2>&1 | grep -q "Up"; then
                echo "📦 Sauvegarde de la base de données..."
                mkdir -p ./backups
                docker-compose -f docker-compose.prod.yml exec -T postgres pg_dump -U ${POSTGRES_USER:-appuser} ${POSTGRES_DB:-nestjs_db} > ./backups/db_$(date +"%Y%m%d_%H%M%S").sql
              fi
            fi

            # Création du répertoire s'il n'existe pas
            mkdir -p ~/app/nestjs

            # Copier les fichiers nécessaires pour l'application NestJS
            rsync -av --exclude 'node_modules' --exclude '.git' /tmp/github-actions-runner/_work/${{ github.repository }}/${{ github.repository }}/ ~/app/nestjs/

            # Utiliser le fichier .env.prod existant sur le VPS
            if [ -f "/app/.env.prod" ]; then
              cp /app/.env.prod ~/app/nestjs/.env
              echo "✅ Fichier .env.prod copié vers .env"
            else
              echo "⚠️ Fichier .env.prod non trouvé à l'emplacement /app/.env.prod"
            fi

            # Se déplacer dans le répertoire de l'application
            cd ~/app/nestjs

            # Construire et démarrer les conteneurs
            docker-compose -f docker-compose.prod.yml down
            docker-compose -f docker-compose.prod.yml up -d --build

            # Attendre que les services soient prêts
            echo "⏳ Attente du démarrage des services..."
            sleep 20

            # Exécuter les migrations TypeORM
            docker-compose -f docker-compose.prod.yml exec -T api npm run migration:run

            # Vérifier que les conteneurs sont bien démarrés
            docker ps

            # Nettoyage
            docker image prune -f